// Importación de Tiposde mongoose
import type { Types } from 'mongoose';

/**
 * Quede pendiente de definir los tipos y luego crear la funcion que hace el registro y luego importarla a la carpeta de la API que contiene lo referente al modulo de cliente
 */